package uz.bank.account.repository;

public class CurrencyQuery {
    private static final String SAVED_CURRENCY_CODE = """
            INSERT INTO account_service.currency VALUES ( id, currencyId, name;)
            """;
}
